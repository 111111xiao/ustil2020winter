* 使用c++语言熟悉类的操作
* 封装 继承 多态
* 自学
* 完成时间截止至2月16日，上传到自己的文件夹中

-- 1.设计带静态成员的学生（Student）类。
要求：
（1）在该类中包括有学号、姓名和成绩，成绩设为有三个课程，如英语、数学和计算机属性；
（2）自行为类添加一个静态成员，例如添加sum和num成员，含义为总分和人数；
（3）应设计学生（Student）与成绩（Score）两个类；
（4）类中必须包含构造函数、拷贝构造函数和析构函数；
（5）为学生类添加静态方法访问静态成员，例如静态方法avg（）进行平均成绩的计算；
（6）完成修改数据，显示数据，修改成绩，统计每个学生的平均成绩操作，而且还能统计多个学生的某一课程平均成绩。

-- 2. 定义一个抽象快递包裹类 Package，并由此派生出普通包裹类和加急包裹类。
要求：
（1）Package 类设计为抽象类，应包含寄件人和收件人姓名、地址和包裹重量属性；
（2）普通包裹增加首重计费和超首重后部分的的每公斤计费单价属性，按首重+超重*每公斤计费单价计算总费用。
（3）加急包裹没 有首重限制，但有加急起价费用属性，按加急起价费+重量*10+加急公里数*0.02 计算总费用。 
（4）为每个类添加 calculateCost 计算快递费用虚函数。编写测试程序，使用指向基类的指针对各个派生类的虚函数进行调用。
